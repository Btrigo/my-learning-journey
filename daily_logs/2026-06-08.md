# Daily Study Log — 2026-06-08

## 📚 What I Worked On
- Watched two Cisco Live 2026 videos on the state of network engineering and AI's role in the field
- Watched Bombal's real-device Wireshark lab covering the TCP/IP model in live traffic
- Watched Bombal's practical subnetting quizzes 1 and 2 using real devices (Windows, Linux, Mac, Cisco router)
- Drilled binary-to-decimal conversion with spaced repetition quizzing
- Watched Bombal's DHCP lab — router as DHCP client and server, switch as DHCP client, real devices
- Reviewed CCNA v1.1 official blueprint subnetting scope across all six domains

## 🧠 What I Learned / Key Takeaways
- CCNA v2.0 goes live February 3, 2027 — finishing v1.1 is the right call; both are equally valid credentials
- v2.0's biggest addition is troubleshooting as a first-class skill; biggest removal is the standalone automation domain
- Binary bit positions run right to left starting at 0: 2⁰=1, 2¹=2, 2²=4 ... 2⁷=128 — bit values are for decimal conversion, powers of 2 are for counting subnet addresses, these are two different tools
- Anything to the power of 0 equals 1
- Subnet mask determines network vs host portion — devices must share the same network portion to communicate without a router
- /25 = 255.255.255.128, splits the last octet in half, two subnets: .0–.127 and .128–.255, 126 usable hosts each
- /28 = 255.255.255.240, 4 host bits, 14 usable addresses — only network address and broadcast are subtracted, not the gateway
- To support 500+ hosts: need 9 host bits (2⁹=512, 512−2=510 usable), minimum prefix is /23
- Wildcard masks used in ACLs are the inverse of subnet masks — flagged as a future topic
- Configuring a Cisco router as DHCP client: `ip address dhcp` on the interface — one command
- Configuring a Cisco router as DHCP server: define exclusion range first, then create pool with network, default-router, and dns-server
- Always exclude the router's own IP from the DHCP pool or it may allocate its own address to a client
- `show ip dhcp binding` shows which MAC addresses received which IPs
- `show ip dhcp pool` shows pool stats: total, leased, excluded addresses
- `traceroute` on Linux requires install; native command is `tracepath`; Windows uses `tracert`

## 🛠 Commands / Tools Practiced
- `show ip interface brief` — interface summary, IP, status/protocol
- `show interface g0/0/1` — detailed interface info including subnet mask in dotted decimal
- `show run interface g0/0/1` — running config for a specific interface, CIDR notation
- `show ip route` — routing table; learned route codes: S*, C, L
- `show run | include dhcp` — filter running config for DHCP-related lines
- `show ip dhcp binding` — lists IP-to-MAC lease assignments
- `show ip dhcp pool` — pool stats and current index
- `conf t` → `int g0/0/1` → `ip address <ip> <mask>` — configure interface IP
- `ip address dhcp` — configure interface as DHCP client
- `ip dhcp excluded-address` — exclude addresses from pool
- `ip dhcp pool <name>` → `network` / `default-router` / `dns-server` / `lease` — DHCP server setup
- `ipconfig` / `ipconfig /all` / `ipconfig /release` / `ipconfig /renew` — Windows
- `ip address` / `ip route` — Linux
- `ifconfig` — Mac
- `tracert` (Windows) / `tracepath` / `traceroute` (Linux/Mac)
- Wireshark — HTTP filter, reading frame layers (L1–L7), identifying MAC, IP, TCP, port info

## ❓ Questions I Still Have / Topics To Review
- Wildcard masks for ACLs — understand how they differ from subnet masks in practice
- Need more hands-on reps with Cisco IOS interface configuration commands in Packet Tracer
- Routing table output — need to get comfortable reading all route codes (C, L, S*, O, etc.)

## 🎯 Plan for Tomorrow
- Redo the DHCP router configuration lab in Packet Tracer from scratch
- Continue Bombal free CCNA playlist (currently on video 19 of 37)